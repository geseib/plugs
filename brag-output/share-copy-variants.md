# Share copy variants — Plugin Catalog

The canonical single caption lives in `share-copy.txt`. These are optional alternates.

## X / Twitter (longer)
Built a one-file, no-backend "app store" for Claude Code plugins. Search 235 plugins by name, tag, or keyword → select → one click drops the exact install commands in your clipboard. Paste into Claude Code. Done. 🧩

## X / Twitter (short)
An app store for Claude Code plugins — in a single HTML file, no backend. Search, select, and the install commands land in your clipboard. 🧩

## LinkedIn
I built Plugin Catalog: a backend-free single-page app that turns any Claude Code plugin marketplace into a searchable, click-to-install catalog. Search 235 plugins by name/tag/keyword, multi-select what you need, and one click copies the exact `claude plugin install … --scope project` commands to your clipboard — paste into Claude Code and you're done. It ships as one HTML file (works on file:// or GitHub Pages) plus a skill that scans any repo and refreshes the catalog. No server, no build step.

## Discord / short
made a no-backend plugin browser for Claude Code — search 235 plugins, click, and it copies the install commands for you. one HTML file. 🧩

## Hashtags
#ClaudeCode #DevTools #AI #plugins
